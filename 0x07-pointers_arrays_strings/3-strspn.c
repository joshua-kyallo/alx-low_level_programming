#include "main.h"


/**
 * _strspn - test function that gets the length of a prefix substring
 * @accept: input(bytes from)
 * @s: input (initial segments)
 * Return: 0
 */

unsigned int _strspn(char *s, char *accept)
{
	unsigned int i, j;

	for (i = 0; s[i] != '\0'; i++)
	{
		for (j = 0; accept[j] != '\0'; j++)
		{
			if (s[i] == accept[j])
			{
				break;
			}
		}
		if (accept[j] == '\0')
		{
			return (i);
		}
	}
	return (i);
}
